services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - SECRET_KEY=change-this-in-production
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
